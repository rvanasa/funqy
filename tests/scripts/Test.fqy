data Bool = F | T

let (^) = sup
let (~) = phf

let _Test = import("_Test")
let Prop = import("examples/Prop")
let Random = import("examples/Random")
let Fourier = import("examples/Fourier")

let f = if 0
	then extract {
		(F, T) => @[1/2] (T, F),
		(T, F) => @[-1/2] (F, T),
	}
	else extract {
		(F, T) => (~T, ~F),
		(T, F) => (~F, ~T),
	}

import("examples/ESVI")
